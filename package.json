{
  "private": true,
  "scripts": {
    "bootstrap": "npx lerna bootstrap --hoist",
    "build": "npx lerna run build",
    "watch": "npx lerna run --parallel watch"
  },
  "devDependencies": {
    "lerna": "3.3.1"
  }
}
